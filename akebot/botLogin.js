// All API crendetials will be here
// The bot only requires a token if you have a bot account
// If you don't want to use a bot account, you can instead use email and password

// *If cleverbot crendentials aren't correct, the bot will crash

module.exports = {
	token: "{Token}",
	email: "{EMAIL}",
	password: "{PASSWORD}",
	cleverBot_API_User: "{API_USER}",
	cleverBot_API_Key: "{API_KEY}"
}